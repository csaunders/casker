<div class="small-10 small-offset-1 large-10 large-offset-1">
    <fieldset>
        <legend><%= festival.name %></legend>

        <p><%= festival.starts_at %> - <%= festival.ends_at %></p>

        <p><%= link_to 'Homepage', festival.website %>

        <p><%= festival.description_html.html_safe %></p>

        <div class="panel">
            <p>Google Map Here</p>
            <p><%= festival.address%></p>
        </div>

    </fieldset>
</div>
