<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Casker" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <% if logged_in? %>
      <div class="contain-to-grid">
        <nav class="top-bar" data-top-bar="navigation">
          <ul class="title-area">
            <li class="name">
              <h1><a href="/">Casker</a></h1>
            </li>
          </ul>

          <% if content_for?(:header) %>
            <%= yield(:header) %>
          <% else %>
            <%= render partial: 'header' %>
          <% end %>

          <%= render partial: 'logout' %>
        </nav>
      </div>
    <% end %>

    <% if flash[:notice] %>
      <div class="row">
        <div class="large-6 large-centered columns text-center">
          <div class="panel callout radius">
            <%= flash[:notice] %>
          </div>
        </div>
      </div>
    <% end %>


    <%= yield %>
    <%= javascript_include_tag "application" %>
  </body>
</html>
